{
  "name": "dofus-fashionista-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Dofus Fashionista mobile app - Capacitor WebView wrapper around https://dofusfashionista.gg",
  "scripts": {
    "sync": "cap sync",
    "open:android": "cap open android",
    "open:ios": "cap open ios",
    "build:android": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
