# Dependances de Dofus Fashionista.
#
# Cette liste est la fermeture calculee par pip a partir des seuls paquets que
# le code importe : Django, PuLP, Pillow, PyMySQL, requests, boto3, jsonpickle,
# Unidecode, social-auth-app-django, social-auth-core, django-htmlmin et
# django-sslserver. Tout le reste ci-dessous est transitif.
#
# Elle a remplace un `pip freeze` de machine qui portait plus de quarante
# paquets de plus,
# dont awk, dbus-python, pyserial, configobj et les dependances de Twisted, que
# rien n'importait. mysql-connector-python en faisait partie : Django passe par
# PyMySQL (`install_as_MySQLdb()` dans fashionsite/__init__.py), pas par lui.
#
# boto3 et botocore doivent rester a la meme version, sinon la construction de
# l'image s'arrete. PinnedDependenciesAgreeTests le verifie.
#
# Le fichier que la production installe reellement (Dockerfile ligne
# 23). gunicorn est ajoute separement par le Dockerfile.

asgiref==3.12.1
beautifulsoup4==4.15.0
boto3==1.43.83
botocore==1.43.87
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
cryptography==50.0.1
defusedxml==0.7.1
django-htmlmin==0.11.0
django-sslserver==0.22
Django==6.0.8
html5lib==1.1
idna==3.19
jmespath==1.1.0
jsonpickle==4.1.2
oauthlib==3.3.1
pillow==12.3.0
PuLP==2.7.0
pycparser==3.0
PyJWT==2.13.0
PyMySQL==1.2.0
python-dateutil==2.9.0.post0
python3-openid==3.2.0
requests-oauthlib==2.0.0
requests==2.34.2
s3transfer==0.19.2
six==1.17.0
social-auth-app-django==6.0.1
social-auth-core==5.1.0
soupsieve==2.9.2
sqlparse==0.6.0
typing_extensions==4.16.0
tzdata==2026.3
Unidecode==1.4.0
urllib3==2.7.0
webencodings==0.6.1
