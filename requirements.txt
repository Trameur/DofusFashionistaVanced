# Dofus Fashionista dependencies.
#
# This list is the closure pip computes from the packages the code imports:
# Django, PuLP, Pillow, PyMySQL, requests, boto3, jsonpickle, Unidecode,
# social-auth-app-django, social-auth-core, django-htmlmin and
# django-sslserver. Everything else below is transitive.
#
# Django reaches MySQL through PyMySQL (`install_as_MySQLdb()` in
# fashionsite/__init__.py), not through mysql-connector-python.
#
# boto3 and botocore must stay on the same version, or the image build stops.
# PinnedDependenciesAgreeTests checks it.
#
# Linux, direct install. Kept identical to the Docker file: two lists
# copied from each other end up drifting apart without anything saying so.

asgiref==3.12.1
beautifulsoup4==4.15.0
boto3==1.43.98
botocore==1.43.98
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
cryptography==50.0.1
defusedxml==0.7.1
django-htmlmin==0.11.0
django-sslserver==0.22
Django==6.0.8
html5lib==1.1
idna==3.20
jmespath==1.1.0
jsonpickle==4.1.2
oauthlib==3.3.1
pillow==12.3.0
PuLP==2.7.0
pycparser==3.0
PyJWT==2.14.0
PyMySQL==1.2.0
python-dateutil==2.9.0.post0
python3-openid==3.2.0
requests-oauthlib==2.0.0
requests==2.34.2
s3transfer==0.19.2
six==1.17.0
social-auth-app-django==6.0.1
social-auth-core==5.1.1
soupsieve==2.9.2
sqlparse==0.6.0
typing_extensions==4.16.0
tzdata==2026.4
Unidecode==1.4.0
urllib3==2.8.0
webencodings==0.6.1
